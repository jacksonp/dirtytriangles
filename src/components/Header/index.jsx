require('./index.css');

import React from 'react'

const Header = () => (
    <header id="dt-header">
        <a href="/">
            <h1>
                <img src="/img/logo.png" alt="Dirty Triangles"/>
            </h1>
        </a>
    </header>
);

export default Header
